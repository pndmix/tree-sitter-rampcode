{
  "name": "tree-sitter-rampcode",
  "version": "0.5.1",
  "description": "Rampcode grammar for tree-sitter",
  "keywords": [
    "tree-sitter",
    "parser",
    "rampcode"
  ],
  "main": "index.js",
  "repository": "https://github.com/pndmix/tree-sitter-rampcode",
  "author": "Reo Matsumoto",
  "license": "MIT",
  "dependencies": {
    "nan": "^2.14.0",
    "prebuild-install": "^5.3.0"
  },
  "devDependencies": {
    "prebuild": "^9.0.1",
    "tree-sitter-cli": "^0.15.7"
  },
  "scripts": {
    "install": "prebuild-install || node-gyp rebuild",
    "prebuild": "prebuild -r electron -t 1.8.0 -t 2.0.0 -t 3.0.0 -t 4.0.0 -t 4.0.4 --strip && prebuild -t 8.16.0 -t 10.12.0 --strip",
    "prebuild:upload": "prebuild --upload-all",
    "generate": "tree-sitter generate && node-gyp build",
    "test": "tree-sitter test"
  }
}
